{%- liquid
  assign current_variant = product.selected_or_first_available_variant
  assign product_form_id = 'product-form-' | append: section.id
  assign brand_name = section.settings.brand_eyebrow | default: shop.name
  assign delivery_text = section.settings.delivery_text | default: 'Delivery in 3-5 Days'
  assign rating_text = section.settings.rating_text | default: '4.9 (428 Reviews)'
  if product.metafields.custom.rating_text != blank
    assign rating_text = product.metafields.custom.rating_text
  endif
  assign orders_text = section.settings.orders_text | default: '2500+ Orders'
  if product.metafields.custom.orders_text != blank
    assign orders_text = product.metafields.custom.orders_text
  endif
  assign promo_heading = section.settings.promo_heading | default: 'Buy 2 Get Free Shipping'
  assign compare_heading = section.settings.compare_heading | default: 'Why Choose Purani Rasoi?'
  assign compare_sub = section.settings.compare_subtext | default: 'Experience the true taste of tradition'
-%}

<div class="pdp" data-pdp data-section-id="{{ section.id }}">
  <div class="pdp__inner max-w-site mx-auto">
    <div class="pdp-layout">
      <section class="pdp-gallery" aria-label="Product images" data-pdp-gallery>
        {%- liquid
          assign thumb_min = 5
          assign media_count = product.media.size
          assign initial_media = product.featured_media
          if product.selected_or_first_available_variant.featured_media != blank
            assign initial_media = product.selected_or_first_available_variant.featured_media
          endif
          assign initial_index = 0
          if initial_media and media_count > 0
            for media in product.media
              if media.id == initial_media.id
                assign initial_index = forloop.index0
              endif
            endfor
          endif
        -%}
        <div class="pdp-gallery__stage pdp-gallery__stage--thumbs">
          <div class="pdp-thumbs" role="list" data-thumbs-rail>
            {%- if media_count > 0 -%}
              {%- for media in product.media -%}
                <button
                  type="button"
                  class="pdp-thumbs__item{% if forloop.index0 == initial_index %} is-active{% endif %}"
                  data-thumb="{{ media | image_url: width: 1400 }}"
                  data-media-id="{{ media.id }}"
                  data-slide-index="{{ forloop.index0 }}"
                  aria-label="View image {{ forloop.index }}"
                >
                  <img src="{{ media | image_url: width: 200 }}" alt="" loading="lazy" width="80" height="80">
                </button>
              {%- endfor -%}
            {%- endif -%}

            {%- if media_count < thumb_min -%}
              {%- assign empty_slots = thumb_min | minus: media_count -%}
              {%- for i in (1..empty_slots) -%}
                <div class="pdp-thumbs__item pdp-thumbs__item--empty" role="listitem" aria-hidden="true">
                  <span class="pdp-thumbs__plus" aria-hidden="true">+</span>
                </div>
              {%- endfor -%}
            {%- endif -%}
          </div>

          <div class="pdp-gallery__main">
            {%- if media_count > 0 -%}
              <div class="pdp-gallery__viewport" data-gallery-viewport>
                <div
                  class="pdp-gallery__track"
                  data-gallery-track
                  style="transform: translateX(-{{ initial_index | times: 100 }}%)"
                >
                  {%- for media in product.media -%}
                    <div class="pdp-gallery__slide" data-gallery-slide data-media-id="{{ media.id }}">
                      <img
                        {% if forloop.index0 == initial_index %}id="pdp-main-img"{% endif %}
                        src="{{ media | image_url: width: 1400 }}"
                        alt="{{ media.alt | escape | default: product.title | escape }}"
                        width="800"
                        height="800"
                        {% unless forloop.index0 == initial_index %}loading="lazy"{% endunless %}
                      >
                    </div>
                  {%- endfor -%}
                </div>
              </div>
            {%- else -%}
              {{ 'product-1' | placeholder_svg_tag: 'pdp-placeholder' }}
            {%- endif -%}
          </div>
        </div>
      </section>

      <section class="pdp-body">
        <p class="pdp-brand">{{ brand_name }}</p>
        <h1 class="pdp-title">{{ product.title }}</h1>

        {%- assign tag_count = 0 -%}
        {%- for block in section.blocks -%}
          {%- if block.type == 'attribute_tag' -%}{%- assign tag_count = tag_count | plus: 1 -%}{%- endif -%}
        {%- endfor -%}
        {%- if tag_count > 0 -%}
          <div class="pdp-tags">
            {%- for block in section.blocks -%}
              {%- if block.type == 'attribute_tag' -%}
                <span class="pdp-tags__item" {{ block.shopify_attributes }}>
                  <span class="material-symbols-outlined" aria-hidden="true">{{ block.settings.icon }}</span>
                  {{ block.settings.label }}
                </span>
              {%- endif -%}
            {%- endfor -%}
          </div>
        {%- endif -%}

        <div class="pdp-meta">
          <div class="pdp-meta__rating">
            <span class="pdp-meta__stars" aria-hidden="true">★★★★★</span>
            <span class="pdp-meta__count">{{ rating_text }}</span>
          </div>
          {%- if orders_text != blank -%}
            <span class="pdp-meta__orders">{{ orders_text }}</span>
          {%- endif -%}
        </div>

        {% form 'product', product, id: product_form_id, class: 'product-form', data-product-form: '' %}
          <input type="hidden" name="id" value="{{ current_variant.id }}" data-variant-id>

          <div class="pdp-price" id="price-{{ section.id }}" data-pdp-price>
            <div class="pdp-price__row">
              <div class="pdp-price__amounts">
                <strong data-price-current>{{ current_variant.price | money }}</strong>
                {%- if current_variant.compare_at_price > current_variant.price -%}
                  <s data-price-compare>{{ current_variant.compare_at_price | money }}</s>
                {%- endif -%}
              </div>
              {%- if current_variant.compare_at_price > current_variant.price -%}
                {%- assign save = current_variant.compare_at_price | minus: current_variant.price | times: 100.0 | divided_by: current_variant.compare_at_price | round -%}
                <span class="pdp-price__off" data-price-off>{{ save }}% OFF</span>
              {%- endif -%}
            </div>
            <p class="pdp-price__tax">(Inclusive of all taxes)</p>
          </div>

          <div class="pdp-availability">
            {%- if product.available -%}
              <span class="pdp-stock" data-pdp-stock>
                <span class="material-symbols-outlined" aria-hidden="true">check_circle</span>
                In Stock
              </span>
            {%- else -%}
              <span class="pdp-stock pdp-stock--out" data-pdp-stock>Sold out</span>
            {%- endif -%}
            {%- if delivery_text != blank -%}
              <span class="pdp-delivery">
                <span class="material-symbols-outlined" aria-hidden="true">local_shipping</span>
                {{ delivery_text }}
              </span>
            {%- endif -%}
          </div>

          <div class="pdp-opts">
            {%- unless product.has_only_default_variant -%}
              {%- for option in product.options_with_values -%}
                <div class="pdp-opts__group">
                  <span class="pdp-label">Select {{ option.name }}</span>
                  <div class="pdp-weights" role="group" aria-label="{{ option.name }}">
                    {%- for value in option.values -%}
                      <button
                        type="button"
                        data-option-value="{{ value | escape }}"
                        data-option-name="{{ option.name | escape }}"
                        data-option-position="{{ option.position }}"
                        {% if option.selected_value == value %}class="is-active"{% endif %}
                      >{{ value }}</button>
                    {%- endfor -%}
                  </div>
                  <select
                    id="Option-{{ section.id }}-{{ forloop.index0 }}"
                    name="options[{{ option.name | escape }}]"
                    data-option-position="{{ option.position }}"
                    class="sr-only"
                  >
                    {%- for value in option.values -%}
                      <option value="{{ value | escape }}" {% if option.selected_value == value %}selected{% endif %}>{{ value }}</option>
                    {%- endfor -%}
                  </select>
                </div>
              {%- endfor -%}
            {%- endunless -%}

            <div class="pdp-opts__group">
              <span class="pdp-label">Quantity</span>
              <div class="pdp-qty" aria-label="{{ 'products.product.quantity' | t }}">
                <button type="button" data-qty-minus aria-label="Decrease quantity">−</button>
                <input
                  id="Quantity-product"
                  type="number"
                  name="quantity"
                  value="1"
                  min="1"
                  class="sr-only"
                  data-qty-input
                >
                <span data-qty-value>1</span>
                <button type="button" data-qty-plus aria-label="Increase quantity">+</button>
              </div>
            </div>
          </div>

          <div class="pdp-ctas">
            <button
              type="submit"
              class="pdp-add"
              name="add"
              data-add-to-cart
              {% unless current_variant.available %}disabled{% endunless %}
            >
              <span class="material-symbols-outlined" aria-hidden="true">shopping_cart</span>
              {%- if current_variant.available -%}
                ADD TO CART
              {%- else -%}
                {{ 'products.product.sold_out' | t }}
              {%- endif -%}
            </button>
            <button
              type="submit"
              class="pdp-buy"
              name="add"
              data-buy-now
              {% unless current_variant.available %}disabled{% endunless %}
            >
              <span class="material-symbols-outlined" aria-hidden="true">bolt</span>
              BUY NOW
            </button>
          </div>
        {% endform %}

        {%- unless section.settings.show_ship_timeline == false -%}
          {%- assign now_s = 'now' | date: '%s' -%}
          {%- assign ship_end_s = now_s | plus: 86400 -%}
          {%- assign del_start_s = now_s | plus: 259200 -%}
          {%- assign del_end_s = now_s | plus: 345600 -%}
          {%- assign d0 = now_s | date: '%d' | plus: 0 -%}
          {%- assign d1 = ship_end_s | date: '%d' | plus: 0 -%}
          {%- assign d2 = del_start_s | date: '%d' | plus: 0 -%}
          {%- assign d3 = del_end_s | date: '%d' | plus: 0 -%}
          {%- capture o0 -%}{%- assign m = d0 | modulo: 10 -%}{%- assign m100 = d0 | modulo: 100 -%}{%- if m100 == 11 or m100 == 12 or m100 == 13 -%}th{%- elsif m == 1 -%}st{%- elsif m == 2 -%}nd{%- elsif m == 3 -%}rd{%- else -%}th{%- endif -%}{%- endcapture -%}
          {%- capture o1 -%}{%- assign m = d1 | modulo: 10 -%}{%- assign m100 = d1 | modulo: 100 -%}{%- if m100 == 11 or m100 == 12 or m100 == 13 -%}th{%- elsif m == 1 -%}st{%- elsif m == 2 -%}nd{%- elsif m == 3 -%}rd{%- else -%}th{%- endif -%}{%- endcapture -%}
          {%- capture o2 -%}{%- assign m = d2 | modulo: 10 -%}{%- assign m100 = d2 | modulo: 100 -%}{%- if m100 == 11 or m100 == 12 or m100 == 13 -%}th{%- elsif m == 1 -%}st{%- elsif m == 2 -%}nd{%- elsif m == 3 -%}rd{%- else -%}th{%- endif -%}{%- endcapture -%}
          {%- capture o3 -%}{%- assign m = d3 | modulo: 10 -%}{%- assign m100 = d3 | modulo: 100 -%}{%- if m100 == 11 or m100 == 12 or m100 == 13 -%}th{%- elsif m == 1 -%}st{%- elsif m == 2 -%}nd{%- elsif m == 3 -%}rd{%- else -%}th{%- endif -%}{%- endcapture -%}
          <div class="pdp-ship" aria-label="Order and delivery timeline">
            <p class="pdp-ship__trust">
              {{ section.settings.ship_trust_text | default: 'Trusted by 1.8 lakh+ Indians | Secure Checkout 100% Genuine Product.' }}
            </p>
            <ol class="pdp-ship__track">
              <li class="pdp-ship__step">
                <span class="pdp-ship__icon" aria-hidden="true">
                  <span class="material-symbols-outlined">shopping_cart</span>
                </span>
                <span class="pdp-ship__date">{{ now_s | date: '%b' }} {{ d0 }}{{ o0 }}</span>
                <span class="pdp-ship__label">Order Placed</span>
              </li>
              <li class="pdp-ship__step">
                <span class="pdp-ship__icon" aria-hidden="true">
                  <span class="material-symbols-outlined">local_shipping</span>
                </span>
                <span class="pdp-ship__date">{{ now_s | date: '%b' }} {{ d0 }}{{ o0 }}-{{ d1 }}{{ o1 }}</span>
                <span class="pdp-ship__label">Order Shipped</span>
              </li>
              <li class="pdp-ship__step">
                <span class="pdp-ship__icon" aria-hidden="true">
                  <span class="material-symbols-outlined">package_2</span>
                </span>
                <span class="pdp-ship__date">{{ del_start_s | date: '%b' }} {{ d2 }}{{ o2 }}-{{ d3 }}{{ o3 }}</span>
                <span class="pdp-ship__label">Estimated Delivery</span>
              </li>
            </ol>
          </div>
        {%- endunless -%}

        {%- assign trust_count = 0 -%}
        {%- for block in section.blocks -%}
          {%- if block.type == 'trust' -%}{%- assign trust_count = trust_count | plus: 1 -%}{%- endif -%}
        {%- endfor -%}
        {%- if trust_count > 0 -%}
          <div class="pdp-trust">
            {%- for block in section.blocks -%}
              {%- if block.type == 'trust' -%}
                <div class="pdp-trust__item" {{ block.shopify_attributes }}>
                  <span class="material-symbols-outlined" aria-hidden="true">{{ block.settings.icon }}</span>
                  <span class="pdp-trust__text">
                    <span class="pdp-trust__title">{{ block.settings.label }}</span>
                    {%- if block.settings.sublabel != blank -%}
                      <span class="pdp-trust__sub">{{ block.settings.sublabel }}</span>
                    {%- endif -%}
                  </span>
                </div>
              {%- endif -%}
            {%- endfor -%}
          </div>
        {%- endif -%}
      </section>
    </div>

    {%- assign feature_count = 0 -%}
    {%- for block in section.blocks -%}
      {%- if block.type == 'feature' -%}{%- assign feature_count = feature_count | plus: 1 -%}{%- endif -%}
    {%- endfor -%}
    {%- if feature_count > 0 -%}
      <section class="pdp-features" aria-label="Key features">
        {%- for block in section.blocks -%}
          {%- if block.type == 'feature' -%}
            <div class="pdp-features__item" {{ block.shopify_attributes }}>
              <span class="pdp-features__icon material-symbols-outlined" aria-hidden="true">{{ block.settings.icon }}</span>
              <span class="pdp-features__label">{{ block.settings.label }}</span>
            </div>
          {%- endif -%}
        {%- endfor -%}
      </section>
    {%- endif -%}

    {%- assign acc_count = 0 -%}
    {%- for block in section.blocks -%}
      {%- if block.type == 'accordion' -%}{%- assign acc_count = acc_count | plus: 1 -%}{%- endif -%}
    {%- endfor -%}
    {%- if acc_count > 0 -%}
      <div class="pdp-acc">
        {%- for block in section.blocks -%}
          {%- if block.type == 'accordion' -%}
            {%- liquid
              assign acc_body = block.settings.body
              if block.settings.metafield != blank
                assign mf_parts = block.settings.metafield | split: '.'
                if mf_parts.size == 2
                  assign mf_value = product.metafields[mf_parts[0]][mf_parts[1]]
                  if mf_value != blank
                    assign acc_body = mf_value
                  endif
                endif
              endif
            -%}
            <details {% if block.settings.open %}open{% endif %} {{ block.shopify_attributes }}>
              <summary>
                <span>{{ block.settings.title }}</span>
                <span class="pdp-acc__chev material-symbols-outlined" aria-hidden="true">expand_more</span>
              </summary>
              <div class="pdp-acc__body">{{ acc_body }}</div>
            </details>
          {%- endif -%}
        {%- endfor -%}
      </div>
    {%- endif -%}

    {%- assign compare_count = 0 -%}
    {%- for block in section.blocks -%}
      {%- if block.type == 'compare_row' -%}{%- assign compare_count = compare_count | plus: 1 -%}{%- endif -%}
    {%- endfor -%}

    {%- if compare_count > 0 -%}
      <section class="pdp-compare" aria-labelledby="Compare-{{ section.id }}">
        <div class="pdp-compare__intro">
          <h2 id="Compare-{{ section.id }}" class="pdp-compare__heading">
            <span class="material-symbols-outlined" aria-hidden="true">eco</span>
            {{ compare_heading }}
          </h2>
          {%- if compare_sub != blank -%}
            <p class="pdp-compare__sub">{{ compare_sub }}</p>
          {%- endif -%}
        </div>
        <div class="pdp-compare__vs">
          <div class="pdp-compare__col pdp-compare__col--us">
            <h3>Purani Rasoi</h3>
            <ul>
              {%- for block in section.blocks -%}
                {%- if block.type == 'compare_row' -%}
                  <li {{ block.shopify_attributes }}>
                    <span class="material-symbols-outlined pdp-compare__yes" aria-hidden="true">check_circle</span>
                    <span>{{ block.settings.us_text | default: block.settings.label }}</span>
                  </li>
                {%- endif -%}
              {%- endfor -%}
            </ul>
          </div>
          <div class="pdp-compare__badge" aria-hidden="true">VS</div>
          <div class="pdp-compare__col pdp-compare__col--them">
            <h3>Other Brands</h3>
            <ul>
              {%- for block in section.blocks -%}
                {%- if block.type == 'compare_row' -%}
                  <li {{ block.shopify_attributes }}>
                    <span class="material-symbols-outlined pdp-compare__no" aria-hidden="true">cancel</span>
                    <span>{{ block.settings.them_text | default: '—' }}</span>
                  </li>
                {%- endif -%}
              {%- endfor -%}
            </ul>
          </div>
        </div>
      </section>
    {%- endif -%}

    {%- assign has_review_apps = false -%}
    {%- for block in section.blocks -%}
      {%- if block.type == '@app' -%}
        {%- assign has_review_apps = true -%}
      {%- endif -%}
    {%- endfor -%}
    {%- if has_review_apps -%}
      <div class="pdp-reviews">
        {%- for block in section.blocks -%}
          {%- if block.type == '@app' -%}
            <div class="pdp-reviews__app" {{ block.shopify_attributes }}>
              {% render block %}
            </div>
          {%- endif -%}
        {%- endfor -%}
      </div>
    {%- endif -%}

    {%- if section.settings.show_promo -%}
      <section class="pdp-promo" data-pdp-promo data-end="{{ section.settings.promo_end }}">
        <div class="pdp-promo__copy">
          <span class="pdp-promo__gift material-symbols-outlined" aria-hidden="true">redeem</span>
          <div>
            <h2>{{ promo_heading }}</h2>
            {%- if section.settings.promo_subtext != blank -%}
              <p>{{ section.settings.promo_subtext }}</p>
            {%- endif -%}
          </div>
        </div>
        <div class="pdp-promo__timer" aria-label="Offer ends in">
          <div class="pdp-promo__unit"><strong data-promo-h>00</strong><span>Hrs</span></div>
          <span class="pdp-promo__sep">:</span>
          <div class="pdp-promo__unit"><strong data-promo-m>00</strong><span>Mins</span></div>
          <span class="pdp-promo__sep">:</span>
          <div class="pdp-promo__unit"><strong data-promo-s>00</strong><span>Secs</span></div>
        </div>
      </section>
    {%- endif -%}

  </div>
</div>

<script type="application/json" id="ProductJSON-{{ section.id }}">
  {{ product | json }}
</script>
<script>
  (function () {
    const root = document.querySelector('[data-pdp][data-section-id="{{ section.id }}"]');
    const form = document.getElementById('{{ product_form_id }}');
    if (!root || !form) return;

    const product = JSON.parse(document.getElementById('ProductJSON-{{ section.id }}').textContent);
    const idInput = form.querySelector('[data-variant-id]');
    const selects = form.querySelectorAll('select[data-option-position]');
    const qtySpan = form.querySelector('[data-qty-value]');
    const qtyInput = form.querySelector('[data-qty-input]');
    const buyNow = form.querySelector('[data-buy-now]');
    const addBtn = form.querySelector('[data-add-to-cart]');
    const track = root.querySelector('[data-gallery-track]');
    const viewport = root.querySelector('[data-gallery-viewport]');
    const slides = Array.from(root.querySelectorAll('[data-gallery-slide]'));
    const stockEl = root.querySelector('[data-pdp-stock]');
    const thumbs = Array.from(root.querySelectorAll('[data-thumb]'));
    let galleryIndex = Math.max(0, thumbs.findIndex((t) => t.classList.contains('is-active')));

    function applyGalleryTransform(instant) {
      if (!track || !slides.length) return;
      if (instant) track.classList.add('is-instant');
      track.style.transform = 'translateX(-' + (galleryIndex * 100) + '%)';
      if (instant) {
        requestAnimationFrame(function () {
          requestAnimationFrame(function () {
            track.classList.remove('is-instant');
          });
        });
      }
    }

    function showGallerySlide(i, instant) {
      if (!slides.length || !track) return;
      const next = (i + slides.length) % slides.length;
      if (next === galleryIndex && !instant) return;
      galleryIndex = next;
      applyGalleryTransform(!!instant);
      thumbs.forEach(function (b, n) {
        b.classList.toggle('is-active', n === galleryIndex);
      });
    }

    function findGalleryIndexForVariant(variant) {
      if (!variant || !slides.length) return -1;
      var mediaId = variant.featured_media && variant.featured_media.id;
      if (!mediaId && variant.featured_image) mediaId = variant.featured_image.id;
      if (mediaId) {
        mediaId = String(mediaId);
        var slideIdx = slides.findIndex(function (slide) {
          return String(slide.getAttribute('data-media-id')) === mediaId;
        });
        if (slideIdx >= 0) return slideIdx;
        var thumbIdx = thumbs.findIndex(function (btn) {
          return String(btn.getAttribute('data-media-id')) === mediaId;
        });
        if (thumbIdx >= 0) return thumbIdx;
      }
      if (variant.featured_image && variant.featured_image.src) {
        var variantFile = variant.featured_image.src.split('/').pop().split('?')[0];
        return thumbs.findIndex(function (btn) {
          var thumbSrc = btn.getAttribute('data-thumb') || '';
          return thumbSrc.split('/').pop().split('?')[0] === variantFile;
        });
      }
      return -1;
    }

    function syncGalleryToVariant(variant) {
      var idx = findGalleryIndexForVariant(variant);
      if (idx >= 0) {
        showGallerySlide(idx, false);
        return;
      }
      if (variant && variant.featured_image) {
        var activeImg = slides[galleryIndex] && slides[galleryIndex].querySelector('img');
        if (activeImg) {
          activeImg.src = variant.featured_image.src.replace(/(\.[a-zA-Z]+)(\?.*)?$/, '_1400x$1');
        }
      }
    }

    function syncQty() {
      if (qtySpan && qtyInput) qtySpan.textContent = qtyInput.value;
    }
    form.querySelector('[data-qty-minus]')?.addEventListener('click', () => {
      qtyInput.value = Math.max(1, (parseInt(qtyInput.value, 10) || 1) - 1);
      syncQty();
    });
    form.querySelector('[data-qty-plus]')?.addEventListener('click', () => {
      qtyInput.value = (parseInt(qtyInput.value, 10) || 1) + 1;
      syncQty();
    });

    function addThen(url) {
      const fd = new FormData(form);
      return fetch('/cart/add.js', {
        method: 'POST',
        body: fd,
        headers: { Accept: 'application/json' },
      }).then(async (r) => {
        if (!r.ok) throw new Error('add failed');
        if (url) {
          window.location.href = url;
          return;
        }
        const cart = await fetch('/cart.js').then((res) => res.json());
        window.PuraniRasoi?.updateCartUI?.(cart);
        window.PuraniRasoi?.openCartDrawer?.();
      });
    }

    buyNow?.addEventListener('click', (e) => {
      e.preventDefault();
      addThen('/checkout').catch(() => form.submit());
    });

    root.querySelectorAll('[data-thumb]').forEach((btn) => {
      btn.addEventListener('click', () => {
        var idx = parseInt(btn.getAttribute('data-slide-index') || String(thumbs.indexOf(btn)), 10);
        showGallerySlide(idx, false);
      });
    });

    if (viewport && slides.length > 1) {
      var touchStartX = 0;
      var touchStartY = 0;
      var swipeThreshold = 40;

      viewport.addEventListener('touchstart', function (e) {
        if (!e.touches.length) return;
        touchStartX = e.touches[0].clientX;
        touchStartY = e.touches[0].clientY;
      }, { passive: true });

      viewport.addEventListener('touchend', function (e) {
        if (!e.changedTouches.length) return;
        var dx = e.changedTouches[0].clientX - touchStartX;
        var dy = e.changedTouches[0].clientY - touchStartY;
        if (Math.abs(dx) < swipeThreshold || Math.abs(dx) < Math.abs(dy)) return;
        if (dx < 0) showGallerySlide(galleryIndex + 1, false);
        else showGallerySlide(galleryIndex - 1, false);
      }, { passive: true });
    }

    form.querySelectorAll('[data-option-value]').forEach((btn) => {
      btn.addEventListener('click', () => {
        const pos = btn.getAttribute('data-option-position');
        const val = btn.getAttribute('data-option-value');
        const select = form.querySelector('select[data-option-position="' + pos + '"]');
        if (!select) return;
        select.value = val;
        form.querySelectorAll('[data-option-position="' + pos + '"][data-option-value]').forEach((b) => {
          b.classList.toggle('is-active', b === btn);
        });
        select.dispatchEvent(new Event('change', { bubbles: true }));
      });
    });

    function selectedOptions() {
      return Array.from(selects).map((s) => s.value);
    }
    function findVariant() {
      const opts = selectedOptions();
      return product.variants.find((v) => v.options.every((o, i) => o === opts[i]));
    }
    function money(cents) {
      if (window.Shopify && Shopify.formatMoney) {
        try {
          return Shopify.formatMoney(cents, {{ shop.money_format | json }});
        } catch (e) {}
      }
      return '₹' + (cents / 100).toFixed(0);
    }
    function update() {
      const v = findVariant() || product.variants[0];
      if (!v || !idInput) return;
      idInput.value = v.id;
      const priceEl = document.getElementById('price-{{ section.id }}');
      if (priceEl) {
        let row = '<div class="pdp-price__row"><div class="pdp-price__amounts"><strong data-price-current">' + money(v.price) + '</strong>';
        if (v.compare_at_price > v.price) {
          const save = Math.round(((v.compare_at_price - v.price) * 100) / v.compare_at_price);
          row += '<s data-price-compare>' + money(v.compare_at_price) + '</s></div><span class="pdp-price__off" data-price-off>' + save + '% OFF</span>';
        } else {
          row += '</div>';
        }
        row += '</div><p class="pdp-price__tax">(Inclusive of all taxes)</p>';
        priceEl.innerHTML = row;
      }
      if (addBtn) {
        addBtn.disabled = !v.available;
        addBtn.innerHTML = v.available
          ? '<span class="material-symbols-outlined" aria-hidden="true">shopping_cart</span> ADD TO CART'
          : {{ 'products.product.sold_out' | t | json }};
      }
      if (buyNow) buyNow.disabled = !v.available;
      if (stockEl) {
        if (v.available) {
          stockEl.classList.remove('pdp-stock--out');
          stockEl.innerHTML = '<span class="material-symbols-outlined" aria-hidden="true">check_circle</span> In Stock';
        } else {
          stockEl.classList.add('pdp-stock--out');
          stockEl.textContent = 'Sold out';
        }
      }
      syncGalleryToVariant(v);
    }
    selects.forEach((s) => s.addEventListener('change', update));

    /* Promo countdown */
    const promo = root.querySelector('[data-pdp-promo]');
    if (promo) {
      const endRaw = promo.getAttribute('data-end');
      const end = endRaw ? new Date(endRaw).getTime() : 0;
      const hEl = promo.querySelector('[data-promo-h]');
      const mEl = promo.querySelector('[data-promo-m]');
      const sEl = promo.querySelector('[data-promo-s]');
      function tick() {
        if (!end) return;
        const diff = Math.max(0, end - Date.now());
        const h = Math.floor(diff / 3600000);
        const m = Math.floor((diff % 3600000) / 60000);
        const s = Math.floor((diff % 60000) / 1000);
        if (hEl) hEl.textContent = String(h).padStart(2, '0');
        if (mEl) mEl.textContent = String(m).padStart(2, '0');
        if (sEl) sEl.textContent = String(s).padStart(2, '0');
      }
      tick();
      setInterval(tick, 1000);
    }
  })();
</script>

{% schema %}
{
  "name": "Product information",
  "settings": [
    { "type": "text", "id": "brand_eyebrow", "label": "Brand eyebrow", "default": "Purani Rasoi" },
    { "type": "text", "id": "rating_text", "label": "Rating text (fallback if no metafield set)", "default": "4.9 (428 Reviews)" },
    { "type": "text", "id": "orders_text", "label": "Orders text (fallback if no metafield set)", "default": "2500+ Orders" },
    { "type": "text", "id": "delivery_text", "label": "Delivery text", "default": "Delivery in 3-5 Days" },
    {
      "type": "checkbox",
      "id": "show_ship_timeline",
      "label": "Show delivery timeline under Buy Now",
      "default": true
    },
    {
      "type": "text",
      "id": "ship_trust_text",
      "label": "Delivery timeline trust line",
      "default": "Trusted by 1.8 lakh+ Indians | Secure Checkout 100% Genuine Product."
    },
    { "type": "text", "id": "compare_heading", "label": "Compare heading", "default": "Why Choose Purani Rasoi?" },
    { "type": "text", "id": "compare_subtext", "label": "Compare subtext", "default": "Experience the true taste of tradition" },
    { "type": "checkbox", "id": "show_promo", "label": "Show promo countdown bar", "default": true },
    { "type": "text", "id": "promo_heading", "label": "Promo heading", "default": "Buy 2 Get Free Shipping" },
    { "type": "text", "id": "promo_subtext", "label": "Promo subtext", "default": "Limited Time Offer!" },
    { "type": "text", "id": "promo_end", "label": "Promo end datetime", "info": "ISO format, e.g. 2026-12-31T23:59:59", "default": "2026-12-31T23:59:59" },
    { "type": "checkbox", "id": "show_related", "label": "Show related products", "default": true },
    { "type": "text", "id": "related_heading", "label": "Related heading", "default": "You May Also Like" },
    {
      "type": "paragraph",
      "content": "Reviews: add a Judge.me (or other) app block in this section via Theme editor → Product information → Add block."
    }
  ],
  "blocks": [
    {
      "type": "@app"
    },
    {
      "type": "media_cert",
      "name": "Media cert chip",
      "settings": [
        { "type": "text", "id": "icon", "label": "Icon", "default": "verified" },
        { "type": "text", "id": "label", "label": "Label", "default": "FSSAI Certified" }
      ]
    },
    {
      "type": "attribute_tag",
      "name": "Attribute tag",
      "settings": [
        { "type": "text", "id": "icon", "label": "Icon", "default": "home" },
        { "type": "text", "id": "label", "label": "Label", "default": "Homemade" }
      ]
    },
    {
      "type": "feature",
      "name": "Feature icon",
      "settings": [
        { "type": "text", "id": "icon", "label": "Icon", "default": "nutrition" },
        { "type": "text", "id": "label", "label": "Label", "default": "Premium Ingredients" }
      ]
    },
    {
      "type": "compare_row",
      "name": "Compare row",
      "settings": [
        { "type": "text", "id": "us_text", "label": "Purani Rasoi point", "default": "Homemade in Small Batches" },
        { "type": "text", "id": "them_text", "label": "Other brands point", "default": "Mass Production" }
      ]
    },
    {
      "type": "trust",
      "name": "Trust badge",
      "settings": [
        { "type": "text", "id": "icon", "label": "Icon", "default": "local_shipping" },
        { "type": "text", "id": "label", "label": "Title", "default": "Free Shipping" },
        { "type": "text", "id": "sublabel", "label": "Subtitle", "default": "on orders above ₹499" }
      ]
    },
    {
      "type": "accordion",
      "name": "Accordion row",
      "settings": [
        { "type": "text", "id": "title", "label": "Title", "default": "Product Highlights" },
        { "type": "richtext", "id": "body", "label": "Body (fallback text, shown when no metafield override is set)", "default": "<ul><li>100% Homemade</li><li>No Artificial Colors</li><li>Made in Small Batches</li></ul>" },
        {
          "type": "text",
          "id": "metafield",
          "label": "Per-product metafield (optional)",
          "info": "e.g. custom.ingredients. When the current product has this metafield filled in, it replaces the Body text above for THIS product only, so every product can show its own content instead of sharing the same text."
        },
        { "type": "checkbox", "id": "open", "label": "Open by default", "default": false }
      ]
    },
  ],
  "presets": [{ "name": "Product information" }]
}
{% endschema %}
